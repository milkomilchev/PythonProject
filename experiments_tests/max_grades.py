def get_max():
    grades = [9.6, 9.2, 9.7]
    maximum = max(grades)
    #values = [float(i) for i in grades]
   # max_local = sum(values)
    return maximum

#max_grades = get_max()
print(get_max())